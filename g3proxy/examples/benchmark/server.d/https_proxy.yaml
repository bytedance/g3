---

name: https_proxy
type: HttpProxy

escaper: default

listen:
  address: "[::]:9080"
  backlog: 4096
listen_in_worker: true

tls_server:
  cert_pairs:
    certificate: "bench.example.net-ec256.crt"
    private_key: "bench.example.net-ec256.key"
